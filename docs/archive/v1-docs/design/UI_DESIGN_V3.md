# UI設計書 V3 - 関東温泉紹介サイト

作成日: 2025年1月  
ベンチマーク: 和倉温泉 あえの風 公式サイト（https://www.aenokaze.jp/）

## 1. 設計方針

### 1.1 ベンチマークサイトの分析

**和倉温泉 あえの風 公式サイトの特徴:**

1. **情報設計**
   - トップページ: ヒーロー画像＋主要メニュー（宿泊、温泉、食事、アクセス、予約）
   - 宿泊プラン: 詳細なプラン一覧（料金、日程、含まれるもの、予約ボタン）
   - 温泉紹介: 温泉の特徴、効能、設備の詳細
   - アクセス: 地図、交通手段、駐車場情報
   - 予約導線: 各ページに明確な予約ボタン

2. **画像の使い方**
   - 高品質な実写画像（温泉、客室、食事）
   - スライダーで複数画像を表示
   - 画像にオーバーレイテキストで情報を追加

3. **モーション・アニメーション**
   - スムーズなスクロールアニメーション
   - ホバーエフェクト（画像ズーム、カードリフト）
   - フェードイン・フェードアウト

4. **予約／問い合わせ導線**
   - ヘッダーに常時表示される予約ボタン
   - 各プランページに予約ボタン
   - 問い合わせフォームへの導線

### 1.2 本サイトへの適用方針

- **世界観**: 「あえの風」の上質感・洗練されたデザインを参考にしつつ、多拠点紹介サイトとして再構成
- **内容**: 温泉ガイド（多拠点紹介・特集・プラン・アクセス）として再構成
- **導線**: 予約サイトへのリンク、問い合わせフォームへの導線を明確化

## 2. サイトマップ（全ページ一覧・URLパス）

### 2.1 ページ構成

```
/
├── / (ホーム)
│   ├── Hero（シネマティックスライダー）
│   ├── エリアから探す（GridGallery）
│   ├── テーマから選ぶ（GridGallery）
│   ├── 旅行者の声（Testimonials）
│   ├── 温泉の選び方（Steps）
│   └── CTA（フルスクリーン）
│
├── /docs (温泉ガイド一覧)
│   └── 温泉地カード一覧（検索・絞り込み機能付き）
│
├── /docs/[slug] (温泉詳細ページ)
│   ├── Hero（温泉地のメイン画像）
│   ├── 概要・魅力（Markdown）
│   ├── 温泉データ
│   │   ├── 地域情報
│   │   ├── 泉質・効能
│   │   ├── 日帰り可否
│   │   └── アクセス情報
│   ├── 宿泊施設一覧（複数）
│   │   ├── 代表的な旅館（既存）
│   │   └── その他の宿泊施設（新規追加）
│   ├── 周辺観光スポット（新規追加）
│   ├── 地図（GoogleMap）
│   ├── 季節ごとの魅力
│   ├── 予約サイトへのリンク（新規追加）
│   └── 前後ナビゲーション
│
├── /blog (特集記事一覧)
│   ├── フィーチャー記事（最初の1件）
│   └── グリッド記事一覧
│
├── /blog/[slug] (記事詳細)
│   ├── Hero（記事のメイン画像）
│   ├── メタ情報（日付、カテゴリ、読了時間）
│   ├── 記事本文（Markdown）
│   └── 関連記事（新規追加）
│
├── /features (おすすめプラン一覧)
│   ├── Hero（プランページのメイン画像）
│   └── プランカード一覧
│
├── /features/[slug] (プラン詳細ページ) [新規追加]
│   ├── Hero（プランのメイン画像）
│   ├── プラン概要
│   ├── 料金情報
│   ├── 日程・スケジュール
│   ├── 含まれるもの
│   ├── 対象温泉地
│   ├── 予約ボタン
│   └── 関連プラン
│
├── /contact (お問い合わせ)
│   ├── お問い合わせフォーム
│   └── 連絡先情報
│
└── /search (検索ページ) [新規追加]
    ├── 検索バー
    ├── 絞り込みフィルタ
    │   ├── エリア
    │   ├── 泉質
    │   ├── 日帰り/宿泊
    │   └── 料金帯
    └── 検索結果一覧
```

### 2.2 URLパス一覧

| パス | ページ名 | 状態 |
|------|---------|------|
| `/` | ホーム | ✅ 既存 |
| `/docs` | 温泉ガイド一覧 | ✅ 既存（検索機能追加予定） |
| `/docs/[slug]` | 温泉詳細 | ✅ 既存（情報拡充予定） |
| `/blog` | 特集記事一覧 | ✅ 既存 |
| `/blog/[slug]` | 記事詳細 | ✅ 既存（関連記事追加予定） |
| `/features` | おすすめプラン一覧 | ✅ 既存 |
| `/features/[slug]` | プラン詳細 | 🆕 新規追加 |
| `/contact` | お問い合わせ | ✅ 既存 |
| `/search` | 検索ページ | 🆕 新規追加 |

## 3. 各ページのセクション構成

### 3.1 ホームページ (`/`)

既存のセクション構成を維持しつつ、以下の改善を実施：

1. **Hero（シネマティックスライダー）**
   - 既存の4スライド（星空、雪、紅葉、新緑）を維持
   - 予約ボタンを追加（「今すぐ予約する」）

2. **エリアから探す（GridGallery）**
   - 既存の6エリア（箱根、草津、鬼怒川、伊香保、那須、水上）を維持
   - カードに「日帰り可」「宿泊推奨」などのバッジを追加

3. **テーマから選ぶ（GridGallery）**
   - 既存の4テーマ（日帰り、カップル、家族、絶景）を維持
   - 各テーマに該当する温泉地の数を表示

4. **旅行者の声（Testimonials）**
   - 既存の2件を維持
   - より具体的なレビューを追加（3-4件に拡充）

5. **温泉の選び方（Steps）**
   - 既存の3ステップを維持

6. **CTA（フルスクリーン）**
   - 既存のCTAを維持
   - 予約ボタンを追加

### 3.2 温泉ガイド一覧 (`/docs`)

既存のページに検索・絞り込み機能を追加：

1. **検索バー**
   - 温泉地名で検索

2. **絞り込みフィルタ**
   - エリア（箱根、草津、鬼怒川、伊香保、那須、水上、その他）
   - 泉質（単純泉、塩化物泉、炭酸水素塩泉、硫酸塩泉、硫黄泉、酸性泉）
   - 日帰り/宿泊（日帰り可、宿泊推奨）
   - 料金帯（〜5,000円、5,000円〜10,000円、10,000円〜、要問い合わせ）

3. **温泉地カード一覧**
   - 既存のカード形式を維持
   - 検索・絞り込み結果に応じて動的に表示

### 3.3 温泉詳細ページ (`/docs/[slug]`)

既存のセクション構成を維持しつつ、以下のセクションを追加：

1. **Hero（既存）**
   - 温泉地のメイン画像
   - タイトル、サブタイトル、説明

2. **概要・魅力（既存）**
   - Markdownコンテンツ

3. **温泉データ（既存）**
   - 地域情報、泉質・効能、日帰り可否、アクセス情報

4. **宿泊施設一覧（拡充）** 🆕
   - 代表的な旅館（既存の1件を維持）
   - その他の宿泊施設（3-5件を追加）
   - 各施設に「予約する」ボタンを追加

5. **周辺観光スポット（新規追加）** 🆕
   - 観光スポット名、説明、画像、アクセス情報

6. **地図（既存）**
   - GoogleMap

7. **季節ごとの魅力（既存）**
   - 春夏秋冬の魅力

8. **予約サイトへのリンク（新規追加）** 🆕
   - 楽天トラベル、じゃらん、一休.comなどのリンク

9. **前後ナビゲーション（既存）**

### 3.4 特集記事一覧 (`/blog`)

既存のページ構成を維持：

1. **ヘッダー**
   - タイトル、説明

2. **フィーチャー記事**
   - 最初の1件を大きく表示

3. **グリッド記事一覧**
   - 残りの記事をグリッド表示

### 3.5 記事詳細ページ (`/blog/[slug]`)

既存のセクション構成を維持しつつ、以下のセクションを追加：

1. **Hero（既存）**
   - 記事のメイン画像
   - タイトル、メタ情報（日付、カテゴリ、読了時間）

2. **記事本文（既存）**
   - Markdownコンテンツ

3. **関連記事（新規追加）** 🆕
   - 同じカテゴリの記事を3件表示

### 3.6 おすすめプラン一覧 (`/features`)

既存のページ構成を維持しつつ、以下の改善を実施：

1. **Hero（既存）**
   - プランページのメイン画像
   - タイトル、説明

2. **プランカード一覧（拡充）**
   - 既存の3プラン（日帰り、カップル、ファミリー）を維持
   - 各カードに「詳細を見る」ボタンを追加
   - 各カードに料金帯を表示

### 3.7 プラン詳細ページ (`/features/[slug]`) 🆕

新規追加するページ：

1. **Hero**
   - プランのメイン画像
   - プラン名、サブタイトル

2. **プラン概要**
   - プランの説明、対象者、おすすめポイント

3. **料金情報**
   - 基本料金、オプション料金、割引情報

4. **日程・スケジュール**
   - 推奨日程、スケジュール例

5. **含まれるもの**
   - 宿泊、食事、温泉、その他のサービス

6. **対象温泉地**
   - このプランが利用可能な温泉地の一覧（リンク付き）

7. **予約ボタン**
   - 予約サイトへのリンク

8. **関連プラン**
   - 他のプランを3件表示

### 3.8 お問い合わせページ (`/contact`)

既存のページ構成を維持：

1. **お問い合わせフォーム**
   - 名前、メールアドレス、メッセージ

2. **連絡先情報**
   - メールアドレス、住所

### 3.9 検索ページ (`/search`) 🆕

新規追加するページ：

1. **検索バー**
   - 温泉地名、キーワードで検索

2. **絞り込みフィルタ**
   - エリア、泉質、日帰り/宿泊、料金帯

3. **検索結果一覧**
   - 検索・絞り込み結果をカード形式で表示
   - ソート機能（人気順、料金順、距離順）

## 4. コンポーネント構成（Next.js / React）

### 4.1 既存コンポーネント（そのまま使用）

| コンポーネント | 場所 | 用途 |
|--------------|------|------|
| `CinematicHero` | `app/components/home/` | Heroスライダー |
| `GridGallery` | `app/components/home/` | エリア・テーマ選択 |
| `Testimonials` | `app/components/home/` | 旅行者の声 |
| `Steps` | `app/components/home/` | 選び方ガイド |
| `CtaFullscreen` | `app/components/home/` | CTAセクション |
| `Header` | `app/components/navigation/` | ヘッダー |
| `Footer` | `app/components/layouts/` | フッター |
| `MarkdownRenderer` | `app/components/ui/` | マークダウン表示 |
| `GoogleMap` | `app/components/ui/` | 地図表示 |
| `ImageCredit` | `app/components/ui/` | 画像クレジット表示 |

### 4.2 新規追加コンポーネント

| コンポーネント | 場所 | 用途 |
|--------------|------|------|
| `SearchBar` | `app/components/search/` | 検索バー |
| `FilterPanel` | `app/components/search/` | 絞り込みフィルタ |
| `AccommodationList` | `app/components/onsen/` | 宿泊施設一覧 |
| `AccommodationCard` | `app/components/onsen/` | 宿泊施設カード |
| `NearbySpots` | `app/components/onsen/` | 周辺観光スポット |
| `BookingLinks` | `app/components/onsen/` | 予約サイトへのリンク |
| `PlanDetail` | `app/components/plans/` | プラン詳細表示 |
| `PlanCard` | `app/components/plans/` | プランカード |
| `RelatedArticles` | `app/components/blog/` | 関連記事 |
| `RelatedPlans` | `app/components/plans/` | 関連プラン |

### 4.3 コンポーネントの詳細設計

#### 4.3.1 `SearchBar` (`app/components/search/SearchBar.tsx`)

```typescript
interface SearchBarProps {
  onSearch: (query: string) => void;
  placeholder?: string;
}
```

- 検索バーコンポーネント
- リアルタイム検索（デバウンス対応）
- 検索候補の表示（オプション）

#### 4.3.2 `FilterPanel` (`app/components/search/FilterPanel.tsx`)

```typescript
interface FilterPanelProps {
  filters: {
    area?: string[];
    springType?: string[];
    dayTrip?: boolean;
    priceRange?: string[];
  };
  onFilterChange: (filters: FilterState) => void;
}
```

- 絞り込みフィルタパネル
- エリア、泉質、日帰り/宿泊、料金帯のフィルタ
- チェックボックス、ラジオボタン、スライダーを使用

#### 4.3.3 `AccommodationList` (`app/components/onsen/AccommodationList.tsx`)

```typescript
interface AccommodationListProps {
  accommodations: Accommodation[];
  onsenSlug: string;
}
```

- 宿泊施設一覧コンポーネント
- 複数の宿泊施設をカード形式で表示
- 各施設に「予約する」ボタンを表示

#### 4.3.4 `AccommodationCard` (`app/components/onsen/AccommodationCard.tsx`)

```typescript
interface AccommodationCardProps {
  accommodation: Accommodation;
  onBookingClick?: (url: string) => void;
}
```

- 宿泊施設カードコンポーネント
- 施設名、画像、特徴、料金帯、予約ボタンを表示

#### 4.3.5 `NearbySpots` (`app/components/onsen/NearbySpots.tsx`)

```typescript
interface NearbySpotsProps {
  spots: NearbySpot[];
}
```

- 周辺観光スポットコンポーネント
- 観光スポット名、説明、画像、アクセス情報を表示

#### 4.3.6 `BookingLinks` (`app/components/onsen/BookingLinks.tsx`)

```typescript
interface BookingLinksProps {
  links: BookingLink[];
  onsenName: string;
}
```

- 予約サイトへのリンクコンポーネント
- 楽天トラベル、じゃらん、一休.comなどのリンクを表示

#### 4.3.7 `PlanDetail` (`app/components/plans/PlanDetail.tsx`)

```typescript
interface PlanDetailProps {
  plan: Plan;
  onBookingClick?: (url: string) => void;
}
```

- プラン詳細表示コンポーネント
- 料金、日程、含まれるもの、対象温泉地を表示

#### 4.3.8 `PlanCard` (`app/components/plans/PlanCard.tsx`)

```typescript
interface PlanCardProps {
  plan: Plan;
  variant?: 'default' | 'featured';
  onDetailClick?: (slug: string) => void;
}
```

- プランカードコンポーネント
- プラン名、説明、料金帯、画像を表示

#### 4.3.9 `RelatedArticles` (`app/components/blog/RelatedArticles.tsx`)

```typescript
interface RelatedArticlesProps {
  currentSlug: string;
  category: string;
  limit?: number;
}
```

- 関連記事コンポーネント
- 同じカテゴリの記事を3件表示

#### 4.3.10 `RelatedPlans` (`app/components/plans/RelatedPlans.tsx`)

```typescript
interface RelatedPlansProps {
  currentSlug: string;
  limit?: number;
}
```

- 関連プランコンポーネント
- 他のプランを3件表示

## 5. JSON Firstで必要になるcontent/textsスキーマ案

### 5.1 `themes/onsen-kanto/content.json` の拡張

#### 5.1.1 `pages.docs[].onsen.accommodation` の拡張

既存の構造を維持しつつ、以下のフィールドを追加：

```json
{
  "accommodation": {
    "dayTripAvailable": true,
    "dayTripFacilities": ["日帰り温泉施設多数"],
    "representativeRyokan": [
      {
        "name": "強羅花壇",
        "features": ["露天風呂", "富士山眺望", "庭園"],
        "priceRange": "1泊2食 30,000円〜",
        "officialUrl": "https://www.gorakadan.com",
        "mapsUrl": "https://www.google.com/maps/search/?api=1&query=強羅花壇+箱根"
      }
    ],
    "otherRyokan": [  // 🆕 新規追加
      {
        "name": "箱根強羅温泉 季の湯 雪月花",
        "features": ["露天風呂", "源泉掛け流し", "レストラン"],
        "priceRange": "1泊2食 25,000円〜",
        "officialUrl": "https://example.com",
        "mapsUrl": "https://www.google.com/maps/search/?api=1&query=季の湯+雪月花+箱根",
        "bookingLinks": [
          {
            "site": "楽天トラベル",
            "url": "https://travel.rakuten.co.jp/..."
          },
          {
            "site": "じゃらん",
            "url": "https://www.jalan.net/..."
          }
        ]
      }
    ],
    "features": ["露天風呂", "貸切風呂", "レストラン", "売店"]
  }
}
```

#### 5.1.2 `pages.docs[].onsen.nearbySpots` の追加 🆕

```json
{
  "nearbySpots": [
    {
      "name": "芦ノ湖",
      "description": "箱根のシンボルとも言える湖。遊覧船やハイキングコースが人気。",
      "image": "lake-ashino",
      "access": "箱根湯本駅からバスで約30分",
      "url": "https://example.com"
    },
    {
      "name": "大涌谷",
      "description": "硫黄の噴煙が立ち上る迫力満点の観光スポット。",
      "image": "owakudani",
      "access": "箱根湯本駅からバスで約40分",
      "url": "https://example.com"
    }
  ]
}
```

#### 5.1.3 `pages.docs[].onsen.bookingLinks` の追加 🆕

```json
{
  "bookingLinks": [
    {
      "site": "楽天トラベル",
      "url": "https://travel.rakuten.co.jp/...",
      "icon": "rakuten"
    },
    {
      "site": "じゃらん",
      "url": "https://www.jalan.net/...",
      "icon": "jalan"
    },
    {
      "site": "一休.com",
      "url": "https://www.ikyu.com/...",
      "icon": "ikyu"
    }
  ]
}
```

#### 5.1.4 `pages.features.items[].details` の追加 🆕

```json
{
  "items": [
    {
      "title": "日帰り温泉プラン",
      "description": "都心から気軽に行ける日帰り温泉コース。",
      "icon": "clock",
      "image": "day-trip",
      "details": {  // 🆕 新規追加
        "price": {
          "base": "2,000円〜",
          "options": [
            {
              "name": "食事付き",
              "price": "+1,500円"
            }
          ],
          "discount": "平日10%割引"
        },
        "schedule": {
          "recommendedDuration": "半日〜1日",
          "example": [
            "10:00 到着・入浴",
            "12:00 ランチ",
            "14:00 再入浴・休憩",
            "16:00 帰路"
          ]
        },
        "includes": [
          "入浴料",
          "タオルレンタル",
          "休憩スペース利用"
        ],
        "targetOnsen": [
          {
            "slug": "hakone",
            "name": "箱根温泉郷"
          },
          {
            "slug": "atami",
            "name": "熱海温泉"
          }
        ],
        "bookingUrl": "https://example.com/booking/day-trip"
      }
    }
  ]
}
```

#### 5.1.5 `pages.blog.posts[].related` の追加 🆕

```json
{
  "posts": [
    {
      "slug": "onsen-manner",
      "title": "知っておきたい温泉マナー10選",
      "related": [  // 🆕 新規追加
        "onsen-effects",
        "seasonal-onsen"
      ]
    }
  ]
}
```

### 5.2 `themes/onsen-kanto/texts.json` の拡張

#### 5.2.1 `booking` セクションの追加 🆕

```json
{
  "booking": {
    "bookNow": "今すぐ予約する",
    "checkAvailability": "空室を確認",
    "viewPrice": "料金を見る",
    "officialSite": "公式サイト",
    "bookingSites": {
      "rakuten": "楽天トラベル",
      "jalan": "じゃらん",
      "ikyu": "一休.com",
      "booking": "Booking.com"
    }
  }
}
```

#### 5.2.2 `plans` セクションの追加 🆕

```json
{
  "plans": {
    "price": "料金",
    "schedule": "日程・スケジュール",
    "includes": "含まれるもの",
    "targetOnsen": "対象温泉地",
    "relatedPlans": "関連プラン",
    "viewDetails": "詳細を見る",
    "basePrice": "基本料金",
    "options": "オプション",
    "discount": "割引情報",
    "recommendedDuration": "推奨日程"
  }
}
```

#### 5.2.3 `accommodation` セクションの追加 🆕

```json
{
  "accommodation": {
    "title": "宿泊施設",
    "representativeRyokan": "代表的な旅館",
    "otherRyokan": "その他の宿泊施設",
    "priceRange": "料金帯",
    "features": "特徴",
    "viewOnMap": "Googleマップで見る",
    "officialSite": "公式サイト",
    "bookNow": "予約する"
  }
}
```

#### 5.2.4 `filters` セクションの追加 🆕

```json
{
  "filters": {
    "title": "絞り込み",
    "area": "エリア",
    "springType": "泉質",
    "dayTrip": "日帰り",
    "accommodation": "宿泊",
    "priceRange": "料金帯",
    "apply": "適用",
    "clear": "クリア",
    "results": "検索結果",
    "noResults": "該当する温泉地が見つかりませんでした"
  }
}
```

#### 5.2.5 `search` セクションの追加 🆕

```json
{
  "search": {
    "placeholder": "温泉地名で検索",
    "noResults": "検索結果が見つかりませんでした",
    "sortBy": "並び替え",
    "sortOptions": {
      "popular": "人気順",
      "price": "料金順",
      "distance": "距離順"
    }
  }
}
```

#### 5.2.6 `nearbySpots` セクションの追加 🆕

```json
{
  "nearbySpots": {
    "title": "周辺観光スポット",
    "access": "アクセス",
    "viewDetails": "詳細を見る"
  }
}
```

#### 5.2.7 `blog` セクションの拡張

```json
{
  "blog": {
    "relatedArticles": "関連記事"  // 🆕 新規追加
  }
}
```

## 6. 型定義の拡張

### 6.1 `app/lib/onsen-types.ts` の拡張

以下の型定義を追加：

```typescript
export interface AccommodationRyokan {
  name: string;
  features: string[];
  priceRange: string;
  officialUrl?: string;
  mapsUrl?: string;
  bookingLinks?: BookingLink[];
}

export interface BookingLink {
  site: string;
  url: string;
  icon?: string;
}

export interface NearbySpot {
  name: string;
  description: string;
  image: string;
  access?: string;
  url?: string;
}

export interface PlanDetails {
  price: {
    base: string;
    options?: Array<{
      name: string;
      price: string;
    }>;
    discount?: string;
  };
  schedule: {
    recommendedDuration: string;
    example?: string[];
  };
  includes: string[];
  targetOnsen: Array<{
    slug: string;
    name: string;
  }>;
  bookingUrl?: string;
}
```

### 6.2 `app/lib/theme-types.ts` の拡張

以下の型定義を追加：

```typescript
export interface TextsConfig {
  // ... 既存のフィールド
  booking?: {
    bookNow: string;
    checkAvailability: string;
    viewPrice: string;
    officialSite: string;
    bookingSites: {
      rakuten: string;
      jalan: string;
      ikyu: string;
      booking: string;
    };
  };
  plans?: {
    price: string;
    schedule: string;
    includes: string;
    targetOnsen: string;
    relatedPlans: string;
    viewDetails: string;
    basePrice: string;
    options: string;
    discount: string;
    recommendedDuration: string;
  };
  accommodation?: {
    title: string;
    representativeRyokan: string;
    otherRyokan: string;
    priceRange: string;
    features: string;
    viewOnMap: string;
    officialSite: string;
    bookNow: string;
  };
  filters?: {
    title: string;
    area: string;
    springType: string;
    dayTrip: string;
    accommodation: string;
    priceRange: string;
    apply: string;
    clear: string;
    results: string;
    noResults: string;
  };
  search?: {
    placeholder: string;
    noResults: string;
    sortBy: string;
    sortOptions: {
      popular: string;
      price: string;
      distance: string;
    };
  };
  nearbySpots?: {
    title: string;
    access: string;
    viewDetails: string;
  };
}
```

## 7. まとめ

### 7.1 設計のポイント

1. **既存の構造を最大限活用**
   - 既存のページ構成、コンポーネント、JSON構造を維持
   - 必要な部分のみ拡張・追加

2. **予約導線の明確化**
   - 各ページに予約ボタンを追加
   - 予約サイトへのリンクを明確に表示

3. **情報の充実**
   - 宿泊施設一覧を複数追加
   - 周辺観光スポットの情報を追加
   - プラン詳細情報を追加

4. **検索・絞り込み機能の追加**
   - エリア、泉質、日帰り/宿泊、料金帯で絞り込み
   - 検索結果のソート機能

### 7.2 実装の優先順位

1. **優先度: 高**
   - プラン詳細ページ (`/features/[slug]`) の追加
   - 宿泊施設一覧の拡充
   - 予約サイトへのリンク追加

2. **優先度: 中**
   - 検索・絞り込み機能の追加
   - 周辺観光スポットの追加
   - 関連記事・関連プランの追加

3. **優先度: 低**
   - UIの細かい調整
   - アニメーションの追加
   - パフォーマンス最適化
