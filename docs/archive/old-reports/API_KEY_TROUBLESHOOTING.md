# APIキー トラブルシューティング

最終更新: 2025-01-XX

## 🔍 現在の状態

- **環境変数の検出**: ❌ GOOGLE_API_KEY / GEMINI_API_KEY が見つかりません
- **google-generativeai**: ✅ インストール済み
- **テストスクリプト**: ✅ 作成済み

## ⚠️ よくある問題

### 1. 別のシェルセッションで設定した場合

環境変数は、設定したシェルセッションでのみ有効です。別のターミナルやシェルで設定した場合、現在のセッションでは見えません。

**解決方法**:
- 現在のシェルセッションで再度設定する
- または `.env` ファイルを使用する

### 2. .env ファイルが読み込まれていない

`.env` ファイルは自動的に読み込まれません。明示的に読み込む必要があります。

**解決方法**:
```bash
# .env ファイルを作成
echo "GOOGLE_API_KEY=your_api_key_here" >> .env

# 読み込んでテスト
source .env
python3 scripts/check-api-key.py
```

## ✅ 確認方法

以下のコマンドで、現在のシェルセッションでAPIキーが設定されているか確認できます：

```bash
# 環境変数を確認（キー自体は表示しない）
python3 scripts/check-api-key.py
```

## 🚀 推奨される設定方法

### 方法1: 現在のシェルセッションで設定

```bash
export GOOGLE_API_KEY=your_api_key_here
python3 scripts/check-api-key.py
```

### 方法2: .env ファイルを使用（推奨）

```bash
# .env ファイルを作成（.gitignoreに追加済み）
echo "GOOGLE_API_KEY=your_api_key_here" >> .env

# 読み込んでテスト
source .env
python3 scripts/check-api-key.py
```

## 📝 次のステップ

APIキーが正しく設定されれば、以下のコマンドで画像検索を実行できます：

```bash
PYTHONPATH=$HOME/.local/lib/python3.12/site-packages:$PYTHONPATH \
  python3 scripts/search-onsen-images-gemini.py
```
