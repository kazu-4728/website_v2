# Theme Schema Documentation

**Version:** 1.0  
**Last Updated:** 2025-12-04

このドキュメントでは、Code Voyage (website_v2) プロジェクトにおける **JSON First アーキテクチャ** の正式なスキーマ仕様を説明します。

---

## 📋 概要

このプロジェクトでは、「**どのテーマでも動く共通の JSON 構造**」を定義し、それに従って各テーマ（例: `onsen-kanto`, `github-docs`, `portfolio` など）を作成します。

各テーマは以下の2つの JSON ファイルで構成されます：

1. **`content.json`** - サイトのコンテンツ（ページ、記事、ナビゲーションなど）
2. **`texts.json`** - UI テキスト（ボタンラベル、エラーメッセージなど）

これらの JSON ファイルが正しい構造を満たしていれば、テーマとして動作します。

---

## 🗂️ ファイル構成

```
themes/
  onsen-kanto/          # 温泉テーマ（例）
    content.json        # コンテンツ定義
    texts.json          # UI テキスト定義
  portfolio/            # ポートフォリオテーマ（将来）
    content.json
    texts.json
  github-docs/          # ドキュメントテーマ（将来）
    content.json
    texts.json
```

---

## 📄 `content.json` スキーマ

`content.json` はサイト全体のコンテンツを定義します。

### トップレベル構造

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `site` | `SiteConfig` | ✅ | サイト全体の設定 |
| `navigation` | `NavigationItem[]` | ✅ | ナビゲーションメニュー |
| `pages` | `PagesConfig` | ✅ | 各ページのコンテンツ |

### `site` - サイト全体の設定

サイト名、タグライン、ロゴ、メタデータを定義します。

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `name` | `string` | ✅ | サイト名 | `"関東温泉紀行"` |
| `tagline` | `string` | ✅ | サイトのキャッチフレーズ | `"Kanto Onsen Journey"` |
| `description` | `string` | ✅ | サイトの説明文 | `"関東エリアの名湯・秘湯を巡る旅..."` |
| `logo.text` | `string` | ✅ | ロゴテキスト | `"温泉紀行"` |
| `logo.icon` | `string` | ✅ | ロゴアイコン識別子 | `"flame"` |
| `metadata.title` | `string` | ✅ | SEO用タイトル | `"関東温泉紀行 \| 名湯・秘湯を巡る旅"` |
| `metadata.description` | `string` | ✅ | SEO用説明文 | 同上 |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

**例:**

```json
{
  "site": {
    "name": "My Portfolio",
    "tagline": "Creative Developer",
    "description": "Showcasing my work and projects",
    "logo": {
      "text": "MP",
      "icon": "code"
    },
    "metadata": {
      "title": "My Portfolio | Creative Developer",
      "description": "Showcasing my work and projects"
    }
  }
}
```

---

### `navigation` - ナビゲーションメニュー

ヘッダーのナビゲーションメニューを定義します。

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `label` | `string` | ✅ | メニュー項目のラベル | `"ホーム"` |
| `href` | `string` | ✅ | リンク先URL | `"/"` |
| `variant` | `'primary' \| 'secondary'` | ❌ | ボタンのスタイル | `"primary"` |
| `submenu` | `NavigationSubmenuItem[]` | ❌ | サブメニュー項目 | `[{ "label": "...", "href": "..." }]` |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

**例:**

```json
{
  "navigation": [
    {
      "label": "Home",
      "href": "/"
    },
    {
      "label": "Projects",
      "href": "/projects",
      "submenu": [
        { "label": "Web Apps", "href": "/projects/web" },
        { "label": "Mobile Apps", "href": "/projects/mobile" }
      ]
    },
    {
      "label": "Contact",
      "href": "/contact",
      "variant": "primary"
    }
  ]
}
```

---

### `pages` - 各ページのコンテンツ

各ページのコンテンツを定義します。

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `home` | `HomePage` | ✅ | ホームページ |
| `docs` | `DocPage[]` | ❌ | ドキュメントページ一覧 |
| `blog` | `BlogConfig` | ❌ | ブログ設定 |
| `features` | `FeaturesConfig` | ❌ | 機能紹介ページ |
| `contact` | `ContactConfig` | ❌ | お問い合わせページ |

---

#### `pages.home` - ホームページ

ホームページは `hero`（ヒーローセクション）と `sections`（セクション一覧）で構成されます。

##### `hero` - ヒーローセクション

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `type` | `string` | ✅ | ヒーローのタイプ | `"cinematic"`, `"simple"`, `"gradient"` |
| `title` | `string` | ✅ | メインタイトル | `"心と体を\n癒す旅へ"` |
| `subtitle` | `string` | ✅ | サブタイトル | `"Healing Journey to..."` |
| `description` | `string` | ✅ | 説明文 | `"都心からわずか数時間..."` |
| `secondaryDescription` | `string` | ❌ | 補足説明文（descriptionの下に表示） | `"関東近郊の厳選温泉を..."` |
| `badges` | `HeroBadge[]` | ❌ | バッジ/タグのリスト | `[{ "label": "週末温泉", "variant": "primary" }]` |
| `bgImage` | `ImageReference` | ✅ | 背景画像 | `"main"` または `{ "key": "main", "keywords": "..." }` |
| `overlay` | `string` | ✅ | オーバーレイスタイル | `"dark"`, `"light"`, `"gradient"` |
| `actions` | `HeroAction[]` | ✅ | アクションボタン | `[{ "label": "...", "href": "...", "variant": "..." }]` |

**HeroBadge:**

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `label` | `string` | ✅ | バッジのラベル | `"週末温泉"`, `"日帰りOK"` |
| `variant` | `string` | ❌ | バッジのスタイル | `"default"`, `"primary"`, `"secondary"` |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

**用途:**
- `secondaryDescription`: メイン説明文の補足や、サイトの特徴を簡潔に伝える
- `badges`: ユーザーの関心を引く短いキーワードやタグを表示（例: 「日帰りOK」「家族で楽しめる」など）

**例:**

```json
{
  "hero": {
    "type": "cinematic",
    "title": "Welcome to My Portfolio",
    "subtitle": "Creative Solutions for Modern Problems",
    "description": "I build beautiful and functional web experiences.",
    "bgImage": { "key": "hero-bg", "keywords": "coding,workspace,modern" },
    "overlay": "dark",
    "actions": [
      { "label": "View Projects", "href": "/projects", "variant": "primary" },
      { "label": "About Me", "href": "/about", "variant": "secondary" }
    ]
  }
}
```

##### `sections` - ホームページのセクション

ホームページには複数のセクションを配置できます。各セクションは `type` フィールドでタイプを指定します。

###### セクションタイプ一覧

| タイプ | 説明 | テーマ共通度 |
|-------|------|------------|
| `split-feature` | 画像とコンテンツを左右に分けて表示 | 🟢 共通 |
| `grid-gallery` | グリッド形式でアイテムを表示 | 🟢 共通 |
| `testimonials` | お客様の声・レビュー | 🟢 共通 |
| `steps` | ステップバイステップガイド | 🟢 共通 |
| `cta-fullscreen` | フルスクリーンのCTA（Call to Action） | 🟢 共通 |

###### `split-feature` セクション

画像とコンテンツを左右に分けて表示するセクション。

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `id` | `string` | ✅ | セクションID（一意） | `"hakone-intro"` |
| `type` | `"split-feature"` | ✅ | セクションタイプ | `"split-feature"` |
| `layout` | `"image-left" \| "image-right"` | ✅ | レイアウト | `"image-right"` |
| `chapter` | `string` | ❌ | チャプター番号 | `"名湯エリア 01"` |
| `title` | `string` | ✅ | タイトル | `"箱根温泉郷"` |
| `subtitle` | `string` | ❌ | サブタイトル | `"Hakone Onsen"` |
| `description` | `string` | ✅ | 説明文 | `"年間2000万人が訪れる..."` |
| `image` | `ImageReference` | ✅ | 画像 | `"hakone-intro"` |
| `stats` | `Array<{ value: string; label: string }>` | ❌ | 統計データ | `[{ "value": "17", "label": "温泉エリア" }]` |
| `quote` | `{ text: string; author: string }` | ❌ | 引用 | `{ "text": "...", "author": "..." }` |
| `link` | `{ text: string; href: string }` | ❌ | リンク | `{ "text": "詳しく見る", "href": "/docs/hakone" }` |

###### `grid-gallery` セクション

グリッド形式で複数のアイテムを表示するセクション。

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `id` | `string` | ✅ | セクションID（一意） |
| `type` | `"grid-gallery"` | ✅ | セクションタイプ |
| `title` | `string` | ✅ | タイトル |
| `subtitle` | `string` | ❌ | サブタイトル |
| `description` | `string` | ✅ | 説明文 |
| `items` | `GridGalleryItem[]` | ✅ | アイテム一覧 |

**GridGalleryItem:**

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `title` | `string` | ✅ | タイトル |
| `description` | `string` | ✅ | 説明文 |
| `image` | `ImageReference` | ✅ | 画像 |
| `href` | `string` | ✅ | リンク先 |

###### `testimonials` セクション

お客様の声やレビューを表示するセクション。

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `id` | `string` | ✅ | セクションID（一意） |
| `type` | `"testimonials"` | ✅ | セクションタイプ |
| `title` | `string` | ✅ | タイトル |
| `items` | `TestimonialItem[]` | ✅ | レビュー一覧 |

**TestimonialItem:**

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `content` | `string` | ✅ | レビュー本文 |
| `author` | `string` | ✅ | 投稿者名 |
| `role` | `string` | ✅ | 投稿者の肩書き |
| `avatar` | `string` | ✅ | アバター画像URL |

###### `cta-fullscreen` セクション

フルスクリーンの Call to Action セクション。

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `id` | `string` | ✅ | セクションID（一意） |
| `type` | `"cta-fullscreen"` | ✅ | セクションタイプ |
| `title` | `string` | ✅ | タイトル |
| `description` | `string` | ✅ | 説明文 |
| `bgImage` | `ImageReference` | ✅ | 背景画像 |
| `action` | `{ label: string; href: string }` | ✅ | アクションボタン |

###### `steps` セクション

ステップバイステップガイドを表示するセクション（例: 「選び方3ステップ」など）。

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `id` | `string` | ✅ | セクションID（一意） |
| `type` | `"steps"` | ✅ | セクションタイプ |
| `title` | `string` | ✅ | タイトル |
| `subtitle` | `string` | ❌ | サブタイトル |
| `description` | `string` | ❌ | 説明文 |
| `items` | `StepItem[]` | ✅ | ステップ一覧 |

**StepItem:**

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `number` | `string` | ✅ | ステップ番号（例: "01", "STEP 1"） |
| `title` | `string` | ✅ | タイトル |
| `description` | `string` | ✅ | 説明文 |
| `icon` | `string` | ❌ | アイコン識別子 |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

---

#### `pages.docs` - ドキュメントページ一覧

ドキュメントページ（例: 温泉ガイド、製品ドキュメントなど）を定義します。

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `slug` | `string` | ✅ | URLスラッグ | `"hakone"` |
| `title` | `string` | ✅ | タイトル | `"箱根温泉郷完全ガイド"` |
| `subtitle` | `string` | ❌ | サブタイトル | `"神奈川県 - Hakone Onsen"` |
| `description` | `string` | ✅ | 説明文 | `"富士箱根伊豆国立公園内に..."` |
| `image` | `ImageReference` | ✅ | メイン画像 | `"hakone"` |
| `content` | `string` | ✅ | Markdown形式の本文 | `"## 箱根温泉の魅力\n\n..."` |
| `related` | `string[]` | ❌ | 関連ドキュメントのスラッグ | `["hakone-yunohana", "hakone-gora"]` |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

**テーマ固有の拡張:**
- 温泉テーマでは `onsen` フィールドを追加可能（後述）

**例:**

```json
{
  "docs": [
    {
      "slug": "getting-started",
      "title": "Getting Started Guide",
      "subtitle": "Quick Start",
      "description": "Learn how to set up and use this project.",
      "image": "getting-started",
      "content": "## Installation\n\nRun `npm install` to get started...",
      "related": ["configuration", "deployment"]
    }
  ]
}
```

---

#### `pages.blog` - ブログ設定

ブログページを定義します。

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `title` | `string` | ✅ | ブログのタイトル |
| `description` | `string` | ✅ | ブログの説明文 |
| `posts` | `BlogPost[]` | ✅ | 記事一覧 |

**BlogPost:**

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `slug` | `string` | ✅ | URLスラッグ | `"onsen-manner"` |
| `title` | `string` | ✅ | タイトル | `"知っておきたい温泉マナー10選"` |
| `excerpt` | `string` | ✅ | 抜粋 | `"初めての温泉旅行でも安心..."` |
| `date` | `string` | ✅ | 公開日 | `"2024.05.15"` |
| `readTime` | `string` | ✅ | 読了時間 | `"5 min"` |
| `category` | `string` | ✅ | カテゴリ | `"マナー"` |
| `author` | `string` | ✅ | 著者名 | `"温泉ソムリエ"` |
| `image` | `ImageReference` | ✅ | アイキャッチ画像 | `"onsen-manner"` |
| `content` | `string` | ✅ | Markdown形式の本文 | `"## 温泉を楽しむための..."` |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

---

#### `pages.features` - 機能紹介ページ

機能やプランを紹介するページを定義します。

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `title` | `string` | ✅ | ページタイトル |
| `description` | `string` | ✅ | ページ説明文 |
| `hero` | `FeaturesHero` | ✅ | ヒーローセクション |
| `items` | `FeatureItem[]` | ✅ | 機能アイテム一覧 |

**FeaturesHero:**

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `title` | `string` | ✅ | タイトル |
| `subtitle` | `string` | ✅ | サブタイトル |
| `description` | `string` | ✅ | 説明文 |
| `image` | `ImageReference` | ✅ | 背景画像 |

**FeatureItem:**

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `title` | `string` | ✅ | タイトル |
| `description` | `string` | ✅ | 説明文 |
| `icon` | `string` | ✅ | アイコン識別子 |
| `image` | `ImageReference` | ✅ | 画像 |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

---

#### `pages.contact` - お問い合わせページ

お問い合わせページを定義します。

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `title` | `string` | ✅ | ページタイトル |
| `description` | `string` | ❌ | ページ説明文 |
| `email` | `string` | ✅ | 問い合わせ先メールアドレス |
| `office` | `string` | ✅ | オフィス住所 |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

---

## 📄 `texts.json` スキーマ

`texts.json` は UI に表示されるテキスト（ボタンラベル、エラーメッセージなど）を一元管理します。

### トップレベル構造

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `nav` | `TextsNavigation` | ✅ | ナビゲーション関連テキスト |
| `pages` | `TextsPages` | ✅ | ページ固有テキスト |
| `buttons` | `TextsButtons` | ✅ | ボタンラベル |
| `form` | `TextsForm` | ✅ | フォーム関連テキスト |
| `messages` | `TextsMessages` | ✅ | メッセージ（エラーなど） |
| `ui` | `TextsUI` | ✅ | UI要素のラベル |

### `nav` - ナビゲーション関連テキスト

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `backLinks.home` | `string` | ✅ | ホームへのバックリンク | `"ホームに戻る"` |
| `backLinks.docs` | `string` | ✅ | ドキュメント一覧へのバックリンク | `"温泉ガイド一覧に戻る"` |
| `backLinks.blog` | `string` | ✅ | ブログ一覧へのバックリンク | `"特集記事一覧に戻る"` |
| `pagination.previous` | `string` | ✅ | 「前へ」ボタン | `"前の温泉地"` |
| `pagination.next` | `string` | ✅ | 「次へ」ボタン | `"次の温泉地"` |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

---

### `pages` - ページ固有テキスト

各ページで使用するテキストを定義します。

**例:**

```json
{
  "pages": {
    "onsenGuide": {
      "title": "温泉ガイド",
      "description": "関東エリアの名湯・秘湯を徹底ガイド...",
      "defaultSubtitle": "Documentation"
    },
    "contact": {
      "title": "お問い合わせ"
    }
  }
}
```

**テーマ共通度:** 🟡 **一部テーマ固有**（キー名はテーマによって異なる可能性あり）

---

### `buttons` - ボタンラベル

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `learnMore` | `string` | ✅ | 「詳しく見る」ボタン | `"詳しく見る"` |
| `readStory` | `string` | ✅ | 「ストーリーを読む」ボタン | `"Read Story"` |
| `learnMoreEn` | `string` | ✅ | 「Learn more」ボタン | `"Learn more"` |
| `submit` | `string` | ✅ | 「送信」ボタン | `"送信する"` |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

---

### `form` - フォーム関連テキスト

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `labels.name` | `string` | ✅ | 名前フィールドのラベル | `"お名前"` |
| `labels.email` | `string` | ✅ | メールフィールドのラベル | `"メールアドレス"` |
| `labels.message` | `string` | ✅ | メッセージフィールドのラベル | `"メッセージ"` |
| `placeholders.name` | `string` | ✅ | 名前フィールドのプレースホルダー | `"山田 太郎"` |
| `placeholders.email` | `string` | ✅ | メールフィールドのプレースホルダー | `"example@email.com"` |
| `placeholders.message` | `string` | ✅ | メッセージフィールドのプレースホルダー | `"お問い合わせ内容を..."` |
| `fields.email.label` | `string` | ✅ | メールフィールド表示用ラベル | `"Email"` |
| `fields.office.label` | `string` | ✅ | オフィスフィールド表示用ラベル | `"Office"` |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

---

### `messages` - メッセージ（エラーなど）

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `notFound.docs` | `string` | ✅ | ドキュメントが見つからない | `"温泉ガイドが見つかりませんでした。"` |
| `notFound.blog` | `string` | ✅ | ブログ記事が見つからない | `"No posts found."` |
| `notFound.blogContent` | `string` | ✅ | ブログコンテンツが見つからない | `"Blog content not found."` |
| `notFound.features` | `string` | ✅ | 機能ページが見つからない | `"Features content not found."` |
| `notFound.contact` | `string` | ✅ | お問い合わせページが見つからない | `"Contact content not found."` |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

---

### `ui` - UI要素のラベル

| フィールド | 型 | 必須 | 説明 | 例 |
|----------|---|------|------|-----|
| `labels.tableOfContents` | `string` | ✅ | 目次 | `"Table of Contents"` |
| `labels.documentation` | `string` | ✅ | ドキュメント | `"Documentation"` |
| `labels.readyToDeploy` | `string` | ✅ | デプロイ準備完了 | `"Ready to Deploy?"` |
| `labels.interactiveDemoLoading` | `string` | ✅ | デモローディング | `"Interactive Demo Module Loading..."` |

**テーマ共通度:** 🟢 **すべてのテーマで共通**

---

## 🎨 `ImageReference` 型

画像は以下の2つの形式で指定できます：

1. **文字列形式:** 画像キーまたは直接URL
   ```json
   "image": "hakone"
   ```

2. **オブジェクト形式:** キーとキーワードを指定
   ```json
   "image": {
     "key": "hakone",
     "keywords": "onsen,hot spring,japan"
   }
   ```

**解決ロジック:**
- 文字列が URL（`http` で始まる）の場合 → そのまま使用
- 文字列がキーの場合 → `data/wikimedia-images.json` または画像管理システムから取得
- オブジェクトの場合 → `key` または `keywords` を使って画像を取得

---

## 🌸 温泉テーマ固有の拡張: `OnsenSpot`

温泉テーマ（`onsen-kanto`）では、`DocPage` に `onsen` フィールドを追加して、温泉地の詳細情報を格納します。

### `onsen` フィールドの構造

| フィールド | 型 | 必須 | 説明 |
|----------|---|------|------|
| `id` | `string` | ✅ | 温泉地ID（一意） |
| `slug` | `string` | ✅ | URLスラッグ（DocPage.slugと一致） |
| `name` | `string` | ✅ | 温泉地名 |
| `nameKana` | `string` | ✅ | ふりがな |
| `nameEn` | `string` | ✅ | 英語名 |
| `region` | `OnsenRegion` | ✅ | 地域情報 |
| `onsen` | `OnsenInfo` | ✅ | 温泉情報 |
| `access` | `OnsenAccess` | ✅ | アクセス情報 |
| `accommodation` | `OnsenAccommodation` | ✅ | 宿泊・施設情報 |
| `content` | `OnsenContent` | ✅ | コンテンツ情報 |
| `images` | `OnsenImages` | ✅ | 画像情報 |
| `metadata` | `OnsenMeta` | ✅ | メタ情報 |
| `mapsUrl` | `string` | ❌ | GoogleマップURL |

**テーマ共通度:** 🔴 **温泉テーマ専用**（他テーマでは不要）

詳細は `app/lib/onsen-types.ts` を参照してください。

---

## 🔧 型定義ファイル

### `app/lib/theme-types.ts`

共通のテーマ型定義が含まれます：

- `TextsConfig` - `texts.json` の型
- `ContentConfig` - `content.json` の型（処理済み）
- `ContentConfigRaw` - `content.json` の型（処理前）
- `SiteConfig`, `NavigationItem`, `HomePage`, `DocPage`, `BlogPost` など

### `app/lib/onsen-types.ts`

温泉テーマ専用の型定義が含まれます：

- `OnsenSpot` - 温泉地データモデル
- `OnsenRegion`, `OnsenInfo`, `OnsenAccess` など
- `OnsenDocPage` - `DocPage` に `onsen` フィールドを追加した型

### `app/lib/content.ts`

コンテンツ読み込みとキャッシュのロジックが含まれます：

- `loadContent()` - `content.json` と `texts.json` を読み込み、`ContentConfig` を返す
- `loadTexts()` - `texts.json` を読み込み、`TextsConfig` を返す
- `getDocPage()`, `getBlogPost()` など

---

## 📝 新しいテーマを作成する手順

1. **ディレクトリ作成:**
   ```bash
   mkdir themes/my-theme
   ```

2. **`content.json` を作成:**
   - 上記のスキーマに従って、`site`, `navigation`, `pages` を定義
   - 必須フィールドを必ず含める

3. **`texts.json` を作成:**
   - 上記のスキーマに従って、UI テキストを定義

4. **`app/lib/content.ts` を更新:**
   - `loadContent()` 関数の `switch` 文に新しいテーマを追加
   ```typescript
   case 'my-theme':
     contentModule = await import('../../themes/my-theme/content.json');
     break;
   ```

5. **環境変数を設定:**
   ```bash
   NEXT_PUBLIC_THEME=my-theme
   ```

6. **ビルドして確認:**
   ```bash
   npm run build
   npm run dev
   ```

---

## ✅ バリデーション

TypeScript の型チェックにより、JSON の構造が正しいかを自動的に検証できます。

ビルド時に型エラーが発生した場合は、JSON の構造を見直してください。

```bash
npm run build
npm run lint
```

---

## 🚀 まとめ

- **`content.json`** → サイトのコンテンツを定義
- **`texts.json`** → UI テキストを定義
- **共通スキーマ** → `app/lib/theme-types.ts` で定義
- **テーマ固有の拡張** → `app/lib/onsen-types.ts` などで定義
- **新しいテーマ作成** → スキーマに従って JSON を作成し、`loadContent()` に追加

このスキーマに従えば、どのテーマでも一貫したアーキテクチャで動作します。

---

**関連ファイル:**

- `app/lib/theme-types.ts` - 共通型定義
- `app/lib/onsen-types.ts` - 温泉テーマ固有型定義
- `app/lib/content.ts` - コンテンツ読み込みロジック
- `themes/onsen-kanto/content.json` - 温泉テーマのコンテンツ
- `themes/onsen-kanto/texts.json` - 温泉テーマの UI テキスト

**更新履歴:**

- 2025-12-04: 初版作成
