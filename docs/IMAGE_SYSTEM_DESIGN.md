# 画像管理システム設計書

## 🎯 設計の目的

テーマごとに画像を管理し、キーワードベースで自動的に適切な画像を取得できるシステムを構築する。

## 📋 実装内容

### 1. 画像管理システム (`app/lib/images.ts`)

- **テーマごとの画像マッピング**: 各テーマ（onsen-kanto, github-docs）ごとに画像を定義
- **キーワードベース検索**: Unsplash Source APIを使用してキーワードから画像を取得
- **画像最適化**: Next.js Image用にURLを最適化

### 2. 画像URL解決システム (`app/lib/content.ts`)

- **`resolveImageUrls`関数**: JSONから読み込んだ画像キーをURLに変換
- **柔軟な形式サポート**:
  - 文字列URL: `"https://images.unsplash.com/..."`
  - キー形式: `"hakone"`
  - オブジェクト形式: `{ "key": "hakone", "keywords": "hakone,onsen,japan" }`

### 3. content.jsonでの画像指定方法

#### 方法1: キー形式（推奨）
```json
{
  "bgImage": { "key": "main", "keywords": "onsen,hot spring,japan" }
}
```

#### 方法2: 直接URL形式（既存の互換性のため）
```json
{
  "bgImage": "https://images.unsplash.com/photo-..."
}
```

#### 方法3: キーワードのみ
```json
{
  "bgImage": { "keywords": "hakone,onsen,japan" }
}
```

## 🔧 画像マッピング

### 温泉テーマ (`onsen-kanto`)

#### ヒーロー画像
- `main`: デフォルトの温泉画像
- `default`: フォールバック画像

#### 温泉地別画像
- `hakone`: 箱根温泉
- `kusatsu`: 草津温泉
- `kinugawa`: 鬼怒川温泉
- その他の温泉地も同様にマッピング

#### セクション画像
- `hakone-intro`: 箱根紹介セクション
- `featured-onsen`: 人気の温泉地セクション

#### CTA画像
- `default`: デフォルトのCTA画像

#### ブログ画像
- `onsen-manner`: 温泉マナーブログ
- `onsen-effects`: 温泉効能ブログ
- `seasonal-onsen`: 季節の温泉ブログ

#### フィーチャー画像
- `day-trip`: 日帰りプラン
- `couple`: カップルプラン
- `family`: ファミリープラン

## 📝 使用方法

### content.jsonでの指定

```json
{
  "pages": {
    "home": {
      "hero": {
        "bgImage": { "key": "main", "keywords": "onsen,hot spring,japan,steam" }
      },
      "sections": [
        {
          "type": "split-feature",
          "image": { "key": "hakone-intro", "keywords": "hakone,onsen,japan" }
        }
      ]
    },
    "docs": [
      {
        "slug": "hakone",
        "image": "hakone"  // キー形式（簡易）
      }
    ]
  }
}
```

### コードでの使用

```typescript
import { getThemeImage, getOnsenImage } from './lib/images';

// テーマ画像を取得
const heroImage = getThemeImage('hero', 'main', 'onsen,hot spring,japan');

// 温泉地画像を取得
const hakoneImage = getOnsenImage('hakone');
```

## ✅ 実装済み機能

1. ✅ テーマごとの画像管理
2. ✅ キーワードベースの画像検索
3. ✅ 画像URLの最適化
4. ✅ 柔軟な画像指定形式のサポート
5. ✅ 後方互換性（既存のURL形式も動作）

## 🔄 次のステップ

1. `content.json`の全ての画像をキー形式に更新
2. 各温泉地に適した画像URLを`images.ts`に追加
3. 画像の視覚確認と調整

## 📚 参考

- Unsplash Source API: `https://source.unsplash.com/{width}x{height}/?{keywords}`
- 画像最適化: Next.js Imageコンポーネント用にURLパラメータを追加
